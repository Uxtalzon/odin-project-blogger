<h1>Find articles with these tags</h1>

<% @tags.each do |tag| %>
	<li>
		<%= link_to tag.name, article_path(tag) %>
		<%= link_to "DELETE", tags_path(@tag), method: :delete, data: {confirm: "Are you sure you want to delete this?"} %>
	</li>
<% end %>